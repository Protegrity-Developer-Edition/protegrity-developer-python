[pytest]
pythonpath = src
bdd_features_base_dir = tests/e2e/features

addopts = --alluredir=allure-results
testpaths =
    tests/e2e
    tests/unit

markers =
    migration: tests covering the Developer Edition → Team Edition migration surface (auth providers, config, request resilience, pty-migrate CLI, usage stats)
